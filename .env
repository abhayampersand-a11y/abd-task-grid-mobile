# Browser testing at localhost:8081. Google accepts a localhost redirect URI
# but rejects a LAN IP, so this is the only local value social sign-in works
# against. Switch to http://172.16.64.27:3000 when testing on a real phone.
EXPO_PUBLIC_API_URL=http://localhost:3000
