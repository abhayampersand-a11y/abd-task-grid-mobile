# expo-router pulls in web-only peers (vaul/radix want react-dom) that a
# native-only install never provides. Without this, plain `npm install` fails
# on an ERESOLVE conflict that has no effect on the native bundle.
legacy-peer-deps=true
